@page
@model FakeStoreWebApp.Pages.ErrorModel

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Error</title>
    <link href="~/css/bootstrap/bootstrap.min.css" rel="stylesheet" />
    <link href="~/css/site.css" rel="stylesheet" asp-append-version="true" />
</head>

<body>
    <div class="main">
        <div class="content px-4">
            <h1 class="text-danger">Error.</h1>
            <h2 class="text-danger">Se ha producido un error al procesar su solicitud.</h2>

            @if (Model.ShowRequestId)
            {
                <p>
                    <strong>ID de solicitud:</strong> <code>@Model.RequestId</code>
                </p>
            }

            <h3>Development Mode</h3>
            <p>
                Al cambiar al entorno <strong>desarrollo</strong> se muestra información detallada sobre el error que se ha producido.
            </p>
            <p>
                <strong>El entorno de desarrollo no debería estar habilitado para las aplicaciones desplegadas.</strong>
                Puede dar lugar a la visualización de información sensible de excepciones a los usuarios finales.
                Para la depuración local, active el entorno <strong>Desarrollo</strong> configurando la opción <strong>ASPNETCORE_ENVIRONMENT</strong> a la variable de entorno <strong>Development</strong>
                y reinicie la aplicación.
            </p>
        </div>
    </div>
</body>

</html>
