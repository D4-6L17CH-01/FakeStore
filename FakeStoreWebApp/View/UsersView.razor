@page "/localhost/users"

@* TODO: La ruta de acceso a esta pagina debe ser /users *@

@inject UsersViewModel ViewModel

<PageTitle>Usuarios</PageTitle>

<h5>Usuarios</h5>

@* TODO: Agregar el componente CommandBar y definir sus respectivas acciones *@
@* TODO: Agregar el componente GridControl y definir sus respectivos parametros y acciones *@


@code {
    protected override async Task OnInitializedAsync()
    {
        ViewModel.StateHasChanged = StateHasChanged;
        await ViewModel.InicializarAsync();
        await base.OnInitializedAsync();
    }
}
